import Spinner from 'react-native-loading-spinner-overlay';

import { styled } from '../../theme';

export const SpinnerStyled = styled(Spinner).attrs(() => ({
  textStyle: {
    color: '#FFF',
    fontSize: 13,
  },
}))``;
